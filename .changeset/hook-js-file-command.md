---
"@moonshot-ai/kimi-code": patch
---

Hook `command` pointing to a `.js`, `.mjs`, or `.cjs` file now runs the file directly in the CLI's built-in Node.js runtime, without needing an external Node.js installation.
